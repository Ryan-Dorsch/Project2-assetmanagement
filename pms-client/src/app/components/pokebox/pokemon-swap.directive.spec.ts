import { PokemonSwapDirective } from './pokemon-swap.directive';

describe('PokemonSwapDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonSwapDirective();
    expect(directive).toBeTruthy();
  });
});
